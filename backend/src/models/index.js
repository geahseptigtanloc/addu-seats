/**
 * Data access layer — Phase 1
 *
 * Prisma schema and generated client live in /backend/prisma/schema.prisma.
 * Query logic will be added here in later phases (e.g. seat queries, reservation CRUD).
 *
 * Import the shared client from config/database.js when writing model helpers.
 */

export { default as prisma } from '../config/database.js';
